<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Audio Recording -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- User Selected Files (for saving audio/history exports) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Network (for downloading Whisper models from Hugging Face) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Hardware -->
    <key>com.apple.security.device.microphone</key>
    <true/>

    <!-- Temporary exception for Accessibility (CGEvent) -->
    <!-- Note: Accessibility permission is requested at runtime via AXIsProcessTrusted() -->
    <key>com.apple.security.temporary-exception.apple-events</key>
    <array>
        <string>com.apple.systemevents</string>
    </array>

    <!-- Hardened Runtime -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <false/>

    <key>com.apple.security.cs.allow-jit</key>
    <false/>

    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <false/>

    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <false/>
</dict>
</plist>
